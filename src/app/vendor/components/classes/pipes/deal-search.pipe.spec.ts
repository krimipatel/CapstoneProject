import { DealSearchPipe } from './deal-search.pipe';

describe('DealSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DealSearchPipe();
    expect(pipe).toBeTruthy();
  });
});